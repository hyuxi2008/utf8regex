language: c
compiler:
 - clang
 - gcc
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install --no-install-recommends -y autoconf autoconf-archive automake libtool
script:
 - ./private/autogen
 - ./configure
 - make
 - ./test
